# 主題輪動指標 — 完整使用與參數說明

> 本文是這個儀表板的「讀圖手冊 + 參數字典 + 編輯指南」，供日後參考。
> 最後更新：2026-08-16（對應 composite 五維度權重 0.25/0.15/0.25/0.25/0.10）。

---

## 目錄

1. [這個指標能預測什麼（預測邊界）](#1-這個指標能預測什麼)
2. [五個維度的定義與參數意義](#2-五個維度的定義與參數意義)
3. [綜合分數 composite 的合成方式](#3-綜合分數-composite)
4. [各圖表的預測用法](#4-各圖表的預測用法)
5. [整合的決策規則](#5-整合的決策規則)
6. [如何編輯 Group 與對應標的](#6-如何編輯-group-與對應標的)
7. [參數調整指南](#7-參數調整指南)
8. [誠實的限制與提醒](#8-誠實的限制與提醒)

---

## 1. 這個指標能預測什麼

**先建立正確的預期，否則會過度解讀。**

| 能力 | 有/無 | 證據（回測 IC） |
|---|---|---|
| 預測「哪個主題**相對**跑贏大盤」 | ✅ 有 | composite IC(20)=+0.35, t=4.07 |
| 預測「主題的**絕對**漲跌」 | ❌ 無 | 指標是橫截面排名，不管市場方向 |
| 短期（約 1 週）預測 | ⚠️ 弱 | IC(5)=+0.13, t=2.56（勉強顯著） |
| 中期（約 1 個月）預測 | ✅ 較強 | IC(20)=+0.35 |

**一句話結論**：這是「**輪動排序器**」，不是「擇時器」。

- 它回答：**「資金接下來傾向流去哪個主題」**
- 它不回答：**「市場要漲還是跌」「這個主題會不會跌」**

**關鍵例子**：記憶體 composite 90 分 = 「記憶體相對其他 7 個主題，未來一個月跑贏的機率較高」——即使大盤這個月跌 10%、記憶體跌 5%，這個預測**依然正確**（因為它跌得比別人少）。

**適用市況**：這個指標在「**板塊輪動明顯**」的行情最有效（例如你最初觀察到的記憶體→光通訊輪動）。在「全市場齊漲齊跌」的市況下，橫截面排序的資訊量會下降。

---

## 2. 五個維度的定義與參數意義

composite 由五個維度加權合成。每個維度的**本質**（它到底在量什麼）比它的名字更重要。

### D1 資金權重（weight = 0.25）
**量「錢實際流去哪」，是純「量」維度。**

| 子成分 | 權重 | 意義 | 公式 |
|---|---|---|---|
| 成交額佔比 | 0.60 | 該主題成交額佔全部主題的比例（橫截面百分位） | 主題成交額 = Σ(成分股收盤×量) |
| 成交量佔比 | 0.40 | 該主題成交量佔全部主題的比例（純量，不含價） | 主題成交量 = Σ(成分股成交量) |

- **成交額** = 錢的「金額」層級（價×量）
- **成交量** = 錢的「換手」層級（純量）
- 兩者都用 `cross_sectional_pct`（橫截面百分位）標準化：8 個主題中排名第 1 得 100、第 8 得 0。
- **預測力**：短期最強（IC(5)=+0.17），中期也強（IC(20)=+0.32）。

### D2 相對強度（weight = 0.15）
**量「價格相對大盤的動量」，是 RRG 風格。**

| 子成分 | 權重 | 意義 |
|---|---|---|
| RS-Ratio 百分位 | 0.50 | 主題指數 / 大盤 的比值 vs 其 20 日均值，>100 相對轉強 |
| RS-Momentum 百分位 | 0.50 | RS-Ratio 的變化率 vs 其 10 日均值，>100 動能上升 |

- 兩個子成分都取橫截面百分位（0–100）。
- **重要實證**：D2 的**短期 IC 不顯著**（IC(5)=+0.04, t=0.92）。RS 窗口掃描證實這是「動量訊號的本質」，換更快窗口無濟於事。因此權重已從 0.40 一路降到 0.15。
- **定位**：D2 是「**中期動量確認**」，不是短期預測工具。

### D3 一致性（weight = 0.25）
**量「主題內部成分股的健康程度」（breadth）。**

- 對**個股籃子**（basket）：`breadth = %成分股站上 20/50/200 日均線 + 20 日正報酬` 的平均（0–100）
- 對 **ETF**（etf）：**設中性 50**（因為 ETF 只有一條價格，算不出「成分股廣度」，0/100 二值與個股籃子的連續比例不可比）

- **預測力**：IC(20)=+0.33，是很強的維度。
- 高 D3 = 主題內多數個股都在上升趨勢，輪動「健康」；低 D3 = 只是少數個股撐場。

### D4 絕對強度（weight = 0.25）
**量「價格是否真的相對強」，補 D2 只看相對排名的盲點。**

- 公式：`D4 = clip(50 + (RS-Ratio原始值 − 100) × scale, 0, 100)`，scale 預設 2
- **RS-Ratio 原始值 > 100** → 真強（D4 > 50）；**< 100** → 真弱（D4 < 50）
- 例：RS-Ratio=110 → D4=70；RS-Ratio=90 → D4=30

- **這是「絕對」訊號**：D2 是「8 個主題裡誰最強」（永遠有人拿 100），D4 是「這個主題本身強不強」（可能大家都 <50，熊市時就是這樣）。
- **預測力**：IC(20)=+0.33，與 D3 並列最強。

### D5 籌碼（weight = 0.10）
**量「量價關係」（價與量的同向/背離），正交於 D1 的量水準與 D2/D4 的價水準。**

| 子成分 | 權重 | 意義 |
|---|---|---|
| 量價確認 | 0.60 | 價漲量增→強勢、價跌量增→恐慌、量縮→動能減弱（純 OHLCV） |
| 換手率變化 | 0.40 | 換手減速→籌碼鎖定（高分）、加速→鬆動（低分） |

- **換手率**需流通股數（`sharesOutstanding`），只有**個股**抓得到；**ETF/缺失設中性 50**。
- **實證**：D5 是五維度中預測力最弱（IC(20)=+0.075），已把權重降到最低 0.10。
- **定位**：參考維度，不單獨作為決策依據。

---

## 3. 綜合分數 composite

```
composite = 0.25×D1資金 + 0.15×D2相對強度 + 0.25×D3一致性 + 0.25×D4絕對 + 0.10×D5籌碼
```

- 各維度都已標準化到 0–100，composite 也是 0–100。
- **權重是回測 IC 驅動的**（不是拍腦袋）：IC 強的維度（D3/D4）權重高，IC 弱的（D2/D5）權重低。

---

## 4. 各圖表的預測用法

### 4.1 排名表（最直接的預測訊號）
**看相對排序，不看絕對值。**
- **Top 2 主題** = 中期（~1 個月）相對跑贏候選
- **搭配 D4 判斷「真強 / 假強」**：
  - composite 高 **且** D4 > 60（RS-Ratio > 105）→ 真強，值得信
  - composite 高 **但** D4 ≈ 50 以下 → 只是「跌得比別人少」，輪動可信度低

### 4.2 RRG 象限圖（領先訊號，最接近「預測」）
RRG 的價值在**位置 + 方向**，看趨勢不是看當下：

| 象限 | 含義 | 預測動作 |
|---|---|---|
| 領先 Leading（右上） | 強 + 動能升 | 已強，可能進入後段 |
| **轉強 Improving（左上）** | 弱但動能轉強 | **最值得關注——「輪動啟動點」** |
| 落後 Lagging（左下） | 弱 + 動能弱 | 迴避 |
| 轉弱 Weakening（右下） | 強但動能轉弱 | 警訊，準備撤 |

**預測上最有價值的不是「現在在哪」，而是「箭頭指向哪」**：
- 從 **Improving → Leading** 的主題 = 資金正在進駐的前兆
- 從 **Weakening → Lagging** 的主題 = 資金正在撤離

三個 RRG 周期對應不同時間尺度（見 §7 參數表）。

### 4.3 三時間框架並排圖（確認趨勢一致性）
三張圖（近 1 月 / 近 2 週 / 近 1 週）對齊看**趨勢是否同向**：
- **三張都往上** → 最可信的「持續流入」訊號
- **長中往上、短週往下** → 短期回調，但中期趨勢未變（優先信中期，因 IC20 > IC5）
- **三張都往下** → 迴避

### 4.4 D 維度分解（判斷訊號「為什麼」強）
點開主題明細抽屜，看哪個 D 在驅動：
- **D1 資金高** → 資金實質進駐（短期最可靠）
- **D4 絕對強度高** → 真材實料的相對強勢（中期最可靠）
- **D2 相對強度** → 別單獨依賴（短期不顯著，僅動量確認）
- **D5 籌碼** → 僅參考

---

## 5. 整合的決策規則

把數據支持的判斷濃縮成三步：

**第一步：找候選** — 排名表 top 3，或 RRG 正在「轉強 Improving」的主題

**第二步：驗證強度** — D4 > 60（真強，非假強）**且** D1 資金高（錢真的在進）

**第三步：確認時間尺度** — 短線看 D1 資金 + 近 1 週圖；中線看 D4 絕對 + 近 1 月圖；**優先信中期（IC20 較強）**

---

## 6. 如何編輯 Group 與對應標的

**所有主題與標的都集中在 `themes.json`，改完 push 後按「更新資料」即可生效。** 不需要改程式碼。

### 6.1 結構

```json
{
  "benchmark": "SPY",                          // 比較基準（大盤）
  "themes": {
    "記憶體": { "type": "basket", "tickers": ["MU", "SNDK", "WDC", "STX"] },
    "半導體/AI": { "type": "etf", "tickers": ["SMH"] }
  }
}
```

### 6.2 `type` 有兩種

| type | 用途 | D3 一致性 | D5 換手率 |
|---|---|---|---|
| `basket` | 自建主題，多檔個股 | 算真 breadth（%成分股站上均線） | 抓流通股數，算真換手率 |
| `etf` | 單一 ETF 代表主題 | 中性 50 | 中性 50 |

> **ETF 為什麼 D3/D5 都中性？** 因為 ETF 只有一條價格，算不出「成分股廣度」，且 ETF 的流通股數 yfinance 抓不到。這是**資料限制**，不是 bug。

### 6.3 新增一個主題

在 `themes` 裡加一組即可：

```json
"themes": {
  ...原有主題...,
  "鋰電池": { "type": "basket", "tickers": ["ALB", "LTHM", "ENVX"] }
}
```

### 6.4 新增單一 ETF 主題

```json
"themes": {
  ...,
  "機器人": { "type": "etf", "tickers": ["BOTZ"] }
}
```

### 6.5 修改現有標的

直接改 `tickers` 陣列，例如把記憶體籃子換成分股：

```json
"記憶體": { "type": "basket", "tickers": ["MU", "WDC", "STX"] }   // 移除 SNDK
```

### 6.6 注意事項

1. **ticker 必須是 yfinance 能抓到的代碼**（美股代碼，如 `MU`、`SMH`）。
2. **新增 ticker 後第一次「更新資料」會抓該檔的歷史**（可能需要幾分鐘），之後就走快取。
3. **主題數量建議 8–15 個**：太少（<5）percentile 排名失真，太多（>20）儀表板線條過雜。
4. **`basket` 建議 3–8 檔成分股**：1–2 檔的 breadth 太二值，>10 檔太雜。
5. **改完記得 commit + push，然後按「更新資料」**。

---

## 7. 參數調整指南

### 7.1 權重（`settings.weights`）

| 鍵 | 對應維度 | 當前值 | 調整依據 |
|---|---|---|---|
| `money` | D1 資金 | 0.25 | 短期預測最強 |
| `strength` | D2 相對強度 | 0.15 | 短期不顯著，已降 |
| `breadth` | D3 一致性 | 0.25 | IC(20) 強 |
| `absolute` | D4 絕對強度 | 0.25 | IC(20) 最強 |
| `chip` | D5 籌碼 | 0.10 | IC 最弱，已降 |

> **權重總和應 = 1.0**。調整後可用 `python backtest_ic.py` 驗證新權重的 IC。

### 7.2 內部權重（`settings.*_internal`）

| 鍵 | 意義 | 當前值 |
|---|---|---|
| `d1_internal.dollar_volume_share` | D1 中「成交額」的權重 | 0.6 |
| `d1_internal.volume_share` | D1 中「成交量」的權重 | 0.4 |
| `d2_internal.rs_ratio` | D2 中 RS-Ratio 權重 | 0.5 |
| `d2_internal.rs_momentum` | D2 中 RS-Momentum 權重 | 0.5 |
| `d5_internal.alignment` | D5 中「量價確認」權重 | 0.6 |
| `d5_internal.turnover` | D5 中「換手率」權重 | 0.4 |

### 7.3 RS 平滑窗口（`settings.rs_ratio_sma` / `rs_momentum_sma`）

| 參數 | 當前值 | 意義 |
|---|---|---|
| `rs_ratio_sma` | 20 | RS-Ratio 的平滑窗口（越大越慢、越平滑） |
| `rs_momentum_sma` | 10 | RS-Momentum 的平滑窗口 |

> ⚠️ **已用回測證實**：D2 的短期無預測力是「動量本質」，不是窗口太慢。`--scan-rs` 掃描顯示換更快窗口（rs=5）短期 IC 反而為負。**不建議改動這兩個值。**

### 7.4 D4 尺度（`settings.abs_strength_scale`）

| 參數 | 當前值 | 意義 |
|---|---|---|
| `abs_strength_scale` | 2 | D4 映射的敏感度：RS-Ratio 每偏離 100 一點，D4 偏離 50 兩點 |

- 值越大，D4 對 RS-Ratio 的偏離越敏感（封頂封底更快到達）。

### 7.5 RRG 三周期的窗口（`rotation.py` 的 `RRG_HORIZONS`）

| 周期 | ratio_sma | momentum_sma | 資料 |
|---|---|---|---|
| 近 1 月（1M） | 20 | 10 | 日線 |
| 近 2 週（2W） | 10 | 5 | 日線 |
| 近 1 週（1W） | 5 | 3 | 5m 盤中 |

> RRG 的平滑窗口決定「RRG 反映多長的周期」。這些是**圖表顯示**的周期，與 composite 的 D2 窗口（20/10）獨立。

---

## 8. 誠實的限制與提醒

1. **置信度上限**：IC +0.35 是「相當好」的輪動訊號（量化界 IC>0.2 即算好），但**單次判斷仍可能錯**（正比率 0.80 = 10 次約 2 次錯）。

2. **小樣本**：8 主題、228 日的 IC 是小樣本估計，換一個市場環境（如 2022 熊市）結論可能不同。

3. **ETF 的限制**：6/8 主題是 ETF，其 D3（一致性）與 D5 換手率都是中性 50——這兩個維度對 ETF 主題**沒有鑑別力**，真正的 breadth/換手率只作用於記憶體、光通訊兩個籃子。

4. **量綱小差異**：D1/D2 是「橫截面百分位」（相對），D3/D4/D5 是「絕對」值，加權合成時量綱略異。這是已知取捨，非 bug。

5. **別用單一圖做決定**：每張圖是同一套數據的不同視角，**交叉驗證**比單看任何一張可靠。

6. **這是研究工具，非交易建議**：指標捕捉「輪動」，不構成買賣訊號，實際交易仍需考慮風險、部位、成本。

---

## 附錄：驗證工具

| 指令 | 用途 |
|---|---|
| `python backtest_ic.py` | 回測各維度的預測力（IC + Newey-West t-stat） |
| `python backtest_ic.py --scan-rs` | 掃描 D2 的 RS 平滑窗口，找最佳參數 |
| `python test_indicators.py` | 指標純函數單元測試 |
| `python test_intraday.py` | 盤中指標單元測試 |
| `python test_datastore.py` | 快取層單元測試 |
| `python export_dashboard.py --from-cache` | 本機重算儀表板資料（不抓網路） |
